class RatingComponent {
  // TODO: constructor sets event listeners? Or should there be a page handler which creates this
  // class and sets the event listeners? Remember, it should do as little as possible.
}